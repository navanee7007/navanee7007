package skb.pages;

public class Homepage extends Logpage {
	 public Homepage logintest(String username,String password)
	   {
	   	uname.sendKeys(username);
	   	upass.sendKeys(password);
	   	ibtn.click();
	   	return new Homepage();
	   	
	   				
	   }

}
